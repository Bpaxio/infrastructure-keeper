server:
  port: 8080

eureka:
  client:
    fetch-registry: true

zuul:
  prefix: "/api"
  strip-prefix: true
  routes:
    files:
#      path: "/files/**"
      serviceId: files-keeper
      strip-prefix: true
    meta:
      serviceId: things-keeper
      strip-prefix: true